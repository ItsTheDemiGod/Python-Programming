#In Python Vaoriables are used to store values that can used and manipulated throughout the program. they act as containers for the data  and can hold different types of data such  as numbers , strings , floats, list, dictionary,etc . the continainer act like as the value it is storing .

# variables are created the moment you assign a value tor them using a assignment operator (=).


age = 20 # integer variable
name = "demian" # string variable
cgpa = 3.4 # float variable
bool= True # boolean variable

# the fact we dont have to specifically declare the type of variable in python makes it a dynamically typed language . 


# rules of variables name in python :
"""
1. variable names must start with a letter (a-z, A-Z) or an underscore (_).
2. variable names can only contain letters, numbers, and underscores.
3. variable names are case-sensitive (age and Age are different variables).
4. variable names cannot be a reserved keyword in Python (like if, else, while, etc.).
"""

"""
32name = "demian" # this will cause a syntax error because variable name cannot start with a number

name = "demian" # this is a valid variable name and assignment

_name = "demian" # this is also a valid variable name and assignment
na@@me = "demian" # this will cause a syntax error because variable name cannot contain special characters like #

print = "hello" # this will cause a syntax error because print is a reserved keyword in python and cannot be used as a variable name """