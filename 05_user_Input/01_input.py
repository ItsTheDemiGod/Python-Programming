# in python we use the input() function to get user input from the console . the input() function takes an optional prompt string as an argument, which is displayed to the user before they enter their input. the input() function returns the user input as a string.


# a = input("enter your name :") 

# here input function is used to get the user input and the prompt string is "enter your name" and then it storing the input in  a variable called a . 

# print(a)


b = input() # we can use the input even without a prompt string but its not a good practise because it can be confusing for the user to know what they are supposed to enter without a prompt.

# by default the input() function always return a string value 

print(b)
print(type(b)) # this will print str even though we entered a number  


